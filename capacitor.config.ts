import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aurora.internalaffairsassistant',
  appName: 'aurora 的内务小猪手',
  webDir: 'dist'
};

export default config;
